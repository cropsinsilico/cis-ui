var chai;

chai = window.chai || require('chai');

describe('Navigation', function() {
  it('renders a thumbnail of graph');
  it('visualizes the viewport area');
  it('allows to pan viewport');
  return it('hides when whole graph is in view');
});

var chai;

chai = window.chai || require('chai');

describe('Thumbnail', function() {
  it('renders a simplified version of graph');
  it('supports dark & light theme');
  it('supports specifying nodeSize');
  return it('supports fill,stroke,edge styles');
});
